import React from "react";

import "../../styles/CarouselTienda.css";

export const Home = () => {
	const { store, actions } = useContext(Context);

	return (
		<div className="text-">
			<h3>Esto es un h3</h3>
		</div>
	); 
};
